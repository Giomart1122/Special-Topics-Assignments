{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C# Debug",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/Debug/net6.0/Program.dll",
            "cwd": "${workspaceFolder}"
        }
    ]
}